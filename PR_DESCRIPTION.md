# Pull Request: 添加一键启动/停止脚本和文档更新

## 📝 变更概述

本次 PR 添加了 Windows 一键启动/停止脚本，优化了周报生成格式，调整了 LLM 配置，并全面更新了项目文档。

## 🎯 主要变更

### 1. ✨ 新增功能

#### 一键启动/停止脚本
- **`start_services.bat`**: Windows 一键启动脚本
  - 自动检测并释放端口 5001 和 5002 的冲突
  - 使用 `pythonw.exe` 后台启动后端（完全无窗口）
  - 使用 `start /B` 后台启动前端
  - 日志输出到 `backend\backend.log` 和 `frontend\frontend.log`
  - 自动打开浏览器访问 http://localhost:5002
  - 启动脚本退出后服务继续运行

- **`stop_services.bat`**: Windows 一键停止脚本
  - 停止端口 5001 和 5002 上的所有进程
  - 清理残留的 Python 和 Node 进程
  - 确保服务完全停止

### 2. 🔧 优化改进

#### 周报格式优化
- **`backend/prompts.py`**: 
  - 移除了系统提示中对序号的要求
  - 明确指示不要在标题前添加序号（1.、2.、3.等）
  - 优化了提示词结构，使输出更规范

- **`backend/generator.py`**: 
  - 添加 `clean_weekly_report_format()` 函数
  - 自动清理 LLM 可能生成的多余序号
  - 使用正则表达式精确匹配并移除 "1. "、"2. " 等模式
  - 保留标题中合理的序号（如 "1、手上项目"）

#### LLM 配置调整
- **超时时间**: 30秒 → 120秒（2分钟）
- **重试次数**: 2次 → 3次
- 总计最多 4 次尝试，总超时时间约 8 分钟
- 解决了 LLM API 响应慢导致的超时问题

### 3. 📚 文档更新

#### README.md (英文)
- 添加"方式一：一键启动脚本"章节，作为 Windows 用户推荐方式
- 更新配置说明表格，增加端口配置说明
- 添加脚本说明章节，详细说明各脚本功能
- 更新周报格式说明，添加格式清理提示

#### README_CN.md (中文)
- 完全重写功能特性章节，更详细地描述功能
- 添加一键启动脚本为首选方式（标注⭐）
- 更新技术栈说明
- 添加详细的配置说明和端口配置
- 更新项目结构说明
- 完善常见问题和故障排除章节

#### 新增文档
- **`QUICK_START.md`**: 详细的快速启动指南
- **`使用说明.txt`**: 简洁的中文快速参考

### 4. 🗑️ 清理

- 删除 `frontend/README.md`: 移除 Create React App 默认的 README 文件
- 更新 `frontend/package-lock.json`: 版本信息更新

## 📊 改动统计

```
8 files changed, 381 insertions(+), 265 deletions(-)
- delete mode 100644 frontend/README.md
- create mode 100644 start_services.bat
- create mode 100644 stop_services.bat
```

## 🔍 技术细节

### 端口配置

| 启动方式 | 后端端口 | 前端端口 |
|---------|---------|---------|
| 脚本启动 | 5001 | 5002 |
| Docker | 5000 | 3000 |

### 格式清理逻辑

```python
def clean_weekly_report_format(report: str) -> str:
    """
    清理周报格式，移除不必要的序号。
    移除类似 "1."、"2."、"3." 等行首序号模式。
    """
    lines = report.split('\n')
    cleaned_lines = []
    
    for line in lines:
        stripped = line.lstrip()
        # 检查是否以"数字."开头
        if re.match(r'^\d+\.\s+', stripped):
            # 移除 "1. " 模式
            cleaned = re.sub(r'^\d+\.\s+', '', stripped)
            cleaned_lines.append(cleaned)
        else:
            # 保持原样
            cleaned_lines.append(line)
    
    return '\n'.join(cleaned_lines)
```

## 🧪 测试

### 测试环境
- Windows 11
- Python 3.x + conda
- Node.js 16+
- PowerShell

### 测试项目
- ✅ 一键启动脚本正常运行
- ✅ 后端服务后台运行（无窗口）
- ✅ 前端服务后台运行
- ✅ 端口自动检测和释放功能正常
- ✅ 日志正确输出到文件
- ✅ 浏览器自动打开
- ✅ 一键停止脚本正常清理所有进程
- ✅ 周报格式清理功能正常
- ✅ LLM 超时配置生效

## 📝 使用示例

### 启动服务
```bash
# 方式1: 双击运行
start_services.bat

# 方式2: 命令行运行
.\start_services.bat
```

### 停止服务
```bash
stop_services.bat
```

### 查看日志
```bash
# 后端日志
type backend\backend.log

# 前端日志
type frontend\frontend.log
```

## 🎯 下一步计划

- [ ] 添加 macOS/Linux 的启动脚本（shell 脚本）
- [ ] 添加服务状态检查脚本
- [ ] 优化日志轮转机制
- [ ] 添加自动更新检查
- [ ] 完善错误处理和用户提示

## ⚠️ 注意事项

1. **Windows 专用**: 当前脚本仅支持 Windows 系统
2. **PowerShell 策略**: 批处理脚本不受 PowerShell 执行策略限制
3. **端口占用**: 脚本会自动处理端口冲突，无需手动清理
4. **后台运行**: 启动脚本退出后服务继续在后台运行
5. **日志文件**: 日志会累积，建议定期清理

## 📋 检查清单

- [x] 代码通过本地测试
- [x] 文档已更新
- [x] 提交信息清晰
- [x] 没有敏感信息
- [x] 向后兼容
- [x] README 已更新

## 🔗 相关链接

- PR 链接: https://github.com/steven140811/Weekly-Report-and-OKR-Assistant/pull/new/feature/update-docs-and-scripts
- Issue: N/A (功能增强)

---

## 👥 Reviewers

@steven140811

## 🏷️ Labels

- `enhancement` - 功能增强
- `documentation` - 文档更新
- `scripts` - 脚本工具
